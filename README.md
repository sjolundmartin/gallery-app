# Aurora HuntR

A gallery app with pictures of the Aurora Borealis. The pictures are fetched using Flickr's open API.

# How to run app

Install dependencies using `npm install`

Then run app using `npm start`

If started locally, the app should be found on `http://localhost:8000`
